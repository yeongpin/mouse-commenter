[ai]
type=ollama
model=qwen2.5:7b
prompt_template=You are an expert code commentator. Analyze the following ${language} code and provide a detailed explanation in ${commentLanguage}. Focus on the purpose, functionality, and important patterns or techniques used. Only add comments inside the code block while preserving the correct commenting style for each language. For HTML, use `<!-- -->` comments. For JavaScript, use `//` for single-line comments and `/* */` for multi-line comments, if in css use /* */ comments. Do not add explanations outside of the code block. \n\n${code}

[openai]
api_key=
model=gpt-4

[ollama]
endpoint=http://192.168.5.54:11434/api/generate
model=qwen2.5:7b